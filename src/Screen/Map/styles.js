import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  map: {
    width: '100%',
    height: '100%',
  },
  backIcon: {
    width: 40,
    height: 40,
  },
  headerContainer: {
    position: 'absolute',
    top: 30,
    left: 30,
    borderWidth: 1,
  },
});

export default styles;
